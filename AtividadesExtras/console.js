let text = 'ASDAWDAWDAdWAD'
let tamanho = text.length

let hora = new Date()
let horaAtual = hora

let frutas = 'Maça, Laranja, Uva, Pera'.split(',')

for (index in frutas) {
    console.log(frutas[index])
}

// Métodos Strings.
// console.log(text.slice(0, 3))
// console.log(frutas.split(','))
//  console.log(frutas.replace('Maça', 'Perâ'))