let text = 'ASDAWDAWDAdWAD'
let tamanho = text.length

let hora = new Date()
let horaAtual = hora

let frutas = 'Maça, Laranja, Uva, Pera'

// Métodos Strings.
console.log(text.slice(0, 3))
console.log(frutas.split(','))